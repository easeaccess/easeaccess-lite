/**
 * Stub for Lite — full bundled translations are a Pro feature.
 * Returns an empty per-locale shape so callers don't crash.
 */
export const allTranslations = { en_US: {} };
export default allTranslations;
