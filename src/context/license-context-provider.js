/**
 * Stub for Lite — there is no license to validate. Wraps children
 * in a fragment so existing callers keep compiling.
 */
const LicenseContextProvider = ({ children }) => children;
export default LicenseContextProvider;
